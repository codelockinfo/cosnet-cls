{
  "product_id": {{ product.id }},
  "metafields": {
    "discount": {% if product.metafields.custom.discount != blank %}{{ product.metafields.custom.discount.value | json }}{% else %}null{% endif %},
    "discount_type": {% if product.metafields.custom.discount_type != blank %}{{ product.metafields.custom.discount_type.value | json }}{% else %}null{% endif %},
    "discount_description": {% if product.metafields.custom.discount_description != blank %}{{ product.metafields.custom.discount_description.value | json }}{% else %}null{% endif %}
  }
}

